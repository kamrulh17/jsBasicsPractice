/* Template literals use back-ticks sign (``). We can add new line anywhere. We can use variables inside a string.We can use mathematical calculations(js expressions) also. */

const country = 'Bangladesh' ;
const newString = `I love ${country}. And it's population is ${100 + 100} million` ;
console.log(newString) ;